JSX - JavaScript in XML
JSX - Expressions
{ JS expression }
{ 2 * 2 }
{ "a" + "b" }
{ "hello".toUpperCase() }
{ 1==1 ? "one" :"two"}
{ 1==1 && <p>you are seeing this</p>}

State:
------


ProductList
  ProductListItem -> 


  code lint


  parent -> child (using props)
  child -> parent ( callback )

  ---------------------------------------
  Forms - in react
  
  controlled 
  uncontrolled 

  -----
  redux
  -----


  react-redux